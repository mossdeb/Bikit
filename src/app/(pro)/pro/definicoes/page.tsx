/**
 * The account's settings, under Bikit Pro's shell: the very page the app
 * shows at /settings, rendered here so the Pro side has its own door to
 * it. One page, two addresses — anything changed there is changed here.
 */
export { default } from "@/app/(app)/settings/page";
