"use client";

/** The app's own, reused on this side of the account: same look, on
 * either side. A client module, as Next wants an error boundary to be. */
export { default } from "@/app/(app)/error";
